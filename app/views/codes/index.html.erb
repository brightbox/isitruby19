

<%= will_paginate @codes, :separator => '&bull;' %>



<% if @codes %>

  <ul>
    <%= render :partial => 'code', :collection => @codes, :locals => { :show_summary => true } %>
  </ul>
<% end %>

<%= will_paginate @codes %>

